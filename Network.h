
// -----------------------------------------------
#define   servername "ucuenca"     // Se define el nombre lógico para el servidor, en este caso http://ucuenca.local
IPAddress local_IP(192, 168, 1, 150); // IP estática del servidor
IPAddress gateway(192, 168, 1, 1);    // Gateway de red, generalmente la dirección base del router
IPAddress subnet(255, 255, 255, 0);   // Máscara de subred
IPAddress dns(192,168,1,1);           // DNS de la red, generalmente la dirección base del router
